# 股票数据源优化升级说明

## 📋 更新概述

为了提高系统的稳定性和可靠性，我们对股票数据获取机制进行了全面优化，添加了多数据源备用机制。

## 🚀 主要改进

### 1. 多数据源备用机制

- **主数据源**: 东财数据源 (`akshare.stock_zh_a_spot_em`)
- **备用数据源1**: 同花顺数据源 (`akshare.stock_zh_a_spot_ths`)
- **备用数据源2**: 腾讯财经数据源 (`akshare.stock_zh_a_spot_tx`)

### 2. 智能数据源切换

当主数据源出现问题时，系统会自动：
1. 检测主数据源是否可用
2. 如果失败，自动切换到同花顺数据源
3. 如果同花顺也失败，再切换到腾讯财经数据源
4. 如果所有数据源都失败，返回错误提示

### 3. 数据格式统一化

不同数据源的字段名称可能不同，系统会自动：
- 统一字段名称格式
- 补充缺失的必要字段
- 确保数据格式一致性

### 4. 数据源信息追踪

- 在缓存文件中记录数据来源
- 在前端显示数据源信息
- 方便问题排查和数据质量评估

## 🔧 技术实现

### 新增函数

#### `get_stock_data_with_fallback()`
```python
def get_stock_data_with_fallback():
    """
    获取股票实时数据，支持多数据源备用机制
    返回: (股票数据DataFrame, 数据源名称)
    """
```

### 字段映射

#### 同花顺数据源字段映射
```python
column_mapping = {
    '股票代码': '代码',
    '股票名称': '名称', 
    '现价': '最新价',
    '涨跌': '涨跌额',
    '涨跌幅': '涨跌幅',
    # ... 更多映射
}
```

#### 腾讯财经数据源字段映射
```python
column_mapping = {
    'code': '代码',
    'name': '名称',
    'price': '最新价',
    'changepercent': '涨跌幅',
    # ... 更多映射
}
```

### 缓存文件格式优化

新的缓存文件包含元数据信息：
```
# 数据来源: 东财数据源
# 生成时间: 2024-09-12 15:30:25
# 股票数量: 156
# ==================================================
代码	名称	最新价	涨跌幅	换手率	...
```

## 📊 系统优势

### 1. 高可用性
- 3个数据源确保系统稳定运行
- 单点故障不会影响整体服务

### 2. 透明度
- 用户可以清楚知道数据来自哪个源
- 便于数据质量评估和问题追踪

### 3. 自动恢复
- 数据源恢复后会自动使用
- 无需人工干预

### 4. 向后兼容
- 不影响现有API接口
- 现有缓存文件仍可正常读取

## 🧪 测试工具

### `test_data_sources.py`
专门的测试脚本，用于：
- 测试各个数据源的可用性
- 验证备用机制工作状态
- 检查数据格式一致性
- 测试完整的筛选流程

使用方法：
```bash
python3 test_data_sources.py
```

## 📱 用户界面改进

### 数据源显示
在前端API响应中，`data_source` 字段现在会显示：
- `"今日缓存 (东财数据源)"`
- `"实时获取 (同花顺数据源)"`
- `"强制刷新 (腾讯财经数据源)"`

## 🔍 监控和调试

### 日志输出示例
```
🌐 正在获取实时股票数据...
📊 尝试使用东财数据源 (akshare.stock_zh_a_spot_em)...
❌ 东财数据源获取失败: 网络连接超时
🔄 切换到同花顺数据源...
📊 尝试使用同花顺数据源 (akshare.stock_zh_a_spot_ths)...
✅ 同花顺数据源获取成功，共 5751 只股票
📊 数据来源: 同花顺数据源
🔢 获取到股票总数: 5751
```

## 🛠️ 配置选项

目前使用固定的数据源优先级，未来可以扩展为：
- 可配置的数据源优先级
- 数据源健康检查
- 负载均衡策略

## 📈 性能影响

- **正常情况**: 无性能影响，仍使用主数据源
- **备用切换**: 增加约2-5秒的切换时间
- **缓存读取**: 新增元数据读取，影响可忽略

## ⚠️ 注意事项

1. **数据源稳定性**: 不同数据源的数据更新频率可能略有差异
2. **字段完整性**: 某些数据源可能缺少特定字段（如量比）
3. **API限制**: 各数据源都有自己的API调用限制

## 🎯 下一步计划

1. **数据源监控**: 添加数据源可用性监控
2. **智能调度**: 根据历史成功率智能选择数据源
3. **性能优化**: 并行请求多个数据源，选择最快响应的
4. **数据校验**: 对比不同数据源的数据一致性

## 📞 问题排查

如果遇到数据获取问题：

1. **运行测试脚本**:
   ```bash
   python3 test_data_sources.py
   ```

2. **检查日志输出**: 查看具体哪个数据源失败

3. **检查网络连接**: 确认可以访问数据源API

4. **检查akshare版本**: 
   ```bash
   pip show akshare
   ```

5. **强制刷新**: 在API中使用 `?refresh=true` 参数

---

**✅ 升级完成！系统现在具备了更强的数据获取稳定性和可靠性。**
